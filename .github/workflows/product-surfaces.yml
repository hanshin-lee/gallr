name: Product surface tests

on:
  pull_request:
    paths:
      - ".github/workflows/product-surfaces.yml"
      - ".editorconfig"
      - ".node-version"
      - ".tool-versions"
      - "admin/**"
      - "androidApp/**"
      - "gallery/**"
      - "web/**"
      - "supabase/config.toml"
      - "supabase/functions/**"
      - "scripts/product-surfaces/**"
      - "vercel.json"
      - "shared/**"
      - "composeApp/**"
      - "iosApp/**"
      - "build.gradle.kts"
      - "settings.gradle.kts"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
  workflow_dispatch:

concurrency:
  group: product-surfaces-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  detect_changes:
    name: Detect changed surfaces
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      web_workspaces: ${{ steps.surfaces.outputs.web_workspaces }}
      public_web: ${{ steps.surfaces.outputs.public_web }}
      edge_functions: ${{ steps.surfaces.outputs.edge_functions }}
      android_shared: ${{ steps.surfaces.outputs.android_shared }}
      ios_compile: ${{ steps.surfaces.outputs.ios_compile }}

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Select affected product surfaces
        id: surfaces
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        shell: python
        run: |
          import json
          import os
          import subprocess

          if os.environ["EVENT_NAME"] == "pull_request":
              changed = set(
                  subprocess.check_output(
                      ["git", "diff", "--name-only", os.environ["BASE_SHA"], os.environ["HEAD_SHA"]],
                      text=True,
                  ).splitlines()
              )
          else:
              changed = {".github/workflows/product-surfaces.yml"}

          run_all = bool(
              changed
              & {
                  ".github/workflows/product-surfaces.yml",
                  ".editorconfig",
              }
          )
          node_global = ".node-version" in changed
          kotlin_global = bool(
              changed
              & {
                  "build.gradle.kts",
                  "settings.gradle.kts",
                  "gradle.properties",
                  "gradlew",
                  "gradlew.bat",
              }
          ) or any(path.startswith("gradle/") for path in changed)

          def under(directory):
              return any(path.startswith(f"{directory}/") for path in changed)

          admin = run_all or node_global or under("admin")
          gallery = run_all or node_global or under("gallery")
          public_web = (
              run_all or node_global or under("web") or "vercel.json" in changed
          )
          edge_functions = (
              run_all
              or ".tool-versions" in changed
              or "supabase/config.toml" in changed
              or under("supabase/functions")
              or under("scripts/product-surfaces")
          )
          shared_mobile = run_all or kotlin_global or under("shared") or under("composeApp")
          android_shared = shared_mobile or under("androidApp")
          ios_compile = shared_mobile or under("iosApp")

          web_workspaces = []
          if admin:
              web_workspaces.append("admin")
          if gallery:
              web_workspaces.append("gallery")

          outputs = {
              "web_workspaces": json.dumps(web_workspaces, separators=(",", ":")),
              "public_web": str(public_web).lower(),
              "edge_functions": str(edge_functions).lower(),
              "android_shared": str(android_shared).lower(),
              "ios_compile": str(ios_compile).lower(),
          }
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              for name, value in outputs.items():
                  output.write(f"{name}={value}\n")

  owner-and-staff-web:
    name: ${{ matrix.workspace }} workspace
    needs: detect_changes
    if: needs.detect_changes.outputs.web_workspaces != '[]'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        workspace: ${{ fromJSON(needs.detect_changes.outputs.web_workspaces) }}

    defaults:
      run:
        working-directory: ${{ matrix.workspace }}

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install pinned Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: ${{ matrix.workspace }}/package-lock.json

      - name: Install locked dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

      - name: Type-check workspace
        run: npm run typecheck

      - name: Build production bundle
        run: npm run build

  public-web:
    name: Public web
    needs: detect_changes
    if: needs.detect_changes.outputs.public_web == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 25

    defaults:
      run:
        working-directory: web

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install pinned Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install locked dependencies
        run: npm ci

      - name: Install Chromium and system dependencies
        run: npx playwright install --with-deps chromium

      - name: Run unit, build, accessibility, and browser suites
        run: npm test

  discover-edge-functions:
    name: Discover Edge functions
    needs: detect_changes
    if: needs.detect_changes.outputs.edge_functions == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      matrix: ${{ steps.functions.outputs.matrix }}

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Test product configuration validator
        run: python3 scripts/product-surfaces/validate-config.test.py

      - name: Validate Auth and Edge Function boundaries
        run: python3 scripts/product-surfaces/validate-config.py

      - name: Build function matrix
        id: functions
        shell: python
        run: |
          import json
          import os
          import re
          from pathlib import Path

          configs = sorted(Path("supabase/functions").glob("*/deno.json"))
          functions = [path.parent.name for path in configs]
          if not functions:
              raise SystemExit("No Edge functions with deno.json were found.")
          invalid = [
              name for name in functions
              if not re.fullmatch(r"[a-z0-9]+(?:-[a-z0-9]+)*", name)
          ]
          if invalid:
              raise SystemExit(f"Invalid Edge function directory names: {invalid}")
          undocumented = [
              path.parent.name for path in configs
              if not (path.parent / "README.md").is_file()
          ]
          if undocumented:
              raise SystemExit(f"Edge functions missing README.md: {undocumented}")
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              output.write(f"matrix={json.dumps(functions)}\n")

  edge-functions:
    name: Edge / ${{ matrix.function }}
    needs: discover-edge-functions
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        function: ${{ fromJSON(needs.discover-edge-functions.outputs.matrix) }}

    defaults:
      run:
        working-directory: supabase/functions/${{ matrix.function }}

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install pinned Deno
        uses: denoland/setup-deno@22d081ff2d3a40755e97629de92e3bcbfa7cf2ed # v2.0.5
        with:
          deno-version-file: .tool-versions
          cache: true

      - name: Run function tests
        run: deno task test

      - name: Check formatting, lint, and types
        run: deno task check

  android-shared:
    name: Android and shared Kotlin
    needs: detect_changes
    if: needs.detect_changes.outputs.android_shared == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Java 17
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "17"

      - name: Configure Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0

      - name: Install Android SDK platform
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4.0.1
        with:
          packages: "platform-tools platforms;android-37.0"

      - name: Check Kotlin style, run unit tests, and lint Android
        run: >-
          ./gradlew
          shared:ktlintCheck
          composeApp:ktlintCheck
          androidApp:ktlintCheck
          shared:testAndroidHostTest
          composeApp:testAndroidHostTest
          androidApp:testDebugUnitTest
          androidApp:lintDebug

  ios-compile:
    name: iOS simulator tests and host build
    needs: detect_changes
    if: needs.detect_changes.outputs.ios_compile == 'true'
    runs-on: macos-15
    timeout-minutes: 30
    env:
      # MapLibre's current XCFramework links an iOS 26 system framework that is
      # unavailable in the macos-15 arm64 image's default Xcode 16.4 SDK.
      DEVELOPER_DIR: /Applications/Xcode_26.3.app/Contents/Developer

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Java 17
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "17"

      - name: Configure Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0

      - name: Resolve iOS map SDK package
        run: xcodebuild -resolvePackageDependencies -project iosApp/iosApp.xcodeproj

      - name: Run iOS Kotlin tests and compile simulator target
        run: >-
          ./gradlew
          shared:iosSimulatorArm64Test
          composeApp:iosSimulatorArm64Test
          composeApp:compileKotlinIosSimulatorArm64

      - name: Build iOS simulator host
        run: >-
          xcodebuild
          -project iosApp/iosApp.xcodeproj
          -scheme iosApp
          -configuration Debug
          -sdk iphonesimulator
          -destination 'generic/platform=iOS Simulator'
          ARCHS=arm64
          ONLY_ACTIVE_ARCH=YES
          CODE_SIGNING_ALLOWED=NO
          build
