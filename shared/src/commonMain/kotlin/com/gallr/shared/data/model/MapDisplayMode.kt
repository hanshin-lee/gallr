package com.gallr.shared.data.model

enum class MapDisplayMode {
    /** Shows only exhibitions matching the current FilterState. */
    FILTERED,

    /** Shows all exhibitions with coordinates, ignoring FilterState. */
    ALL,
}
